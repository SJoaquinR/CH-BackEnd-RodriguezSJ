paths:
  /api/carrito:
    get:
      summary: Obtiene todos los carritos disponibles
      tags:
        - Carrito
      responses:
        "200":
          description: Muestra todos los carritos disponibles.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Cart'